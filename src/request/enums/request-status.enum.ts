export enum RequestStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  ACCEPTED="accepted",
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}

export enum DepositStatus {
  UNPAID = 'unpaid',
  PENDING = 'pending',
  PAID = 'paid',
}