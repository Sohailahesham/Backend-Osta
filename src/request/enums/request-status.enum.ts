export enum RequestStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  IN_PROGRESS = 'in_progress',
  ON_THE_WAY = 'on_the_way',
  STARTED = 'started',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}

export enum DepositStatus {
  UNPAID = 'unpaid',
  PENDING = 'pending',
  PAID = 'paid',
}